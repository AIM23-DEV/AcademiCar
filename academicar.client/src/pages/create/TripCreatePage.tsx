import { TitleBar } from "../../components/TitleBar";

// TODO add content components and follow up pages
export const TripCreatePage = () => {
    return (
        <div>
            <TitleBar text="Neue Reise erstellen" hasBackAction={true} />
        </div>
    );
};
