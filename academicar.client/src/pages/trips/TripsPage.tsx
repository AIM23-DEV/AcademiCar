import { TitleBar } from "../../components/TitleBar";

// TODO add list of trips for the current user
export const TripsPage = () => {
    return (
        <div>
            <TitleBar text="Aktuelle Fahrten" />
        </div>
    );
};
